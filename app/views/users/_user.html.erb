<li>
  <%= gravatar_for user, size: Settings.size_grava %>
  <%= link_to user.name, user %>
  <% if current_user.admin? && !current_user?(user) %>
	  | <%= link_to t("delete_user"), user, method: :delete,data: {confirm: t("access_delete")} %>
  <% end %>
</li>
